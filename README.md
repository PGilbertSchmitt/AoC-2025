# Advent of Code 2025

Simply put all inputs as text files in `src/inputs/` and run `cargo test --release`. Some important notes:

- Day 6 needs to be pasted **_exactly_** as is without the whitespace being touched. The whitespace at the end of each line in that input is important.
